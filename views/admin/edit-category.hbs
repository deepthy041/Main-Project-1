<section>
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h2 class="text-center">Edit category</h2>
                <form action="/admin/edit-category/{{category._id}}" method="post">
                    <label for="">Name of category</label>
                    <input type="text" name="Name" class="form-control" id="Name" value="{{category.Name}}"  onkeyup="validateName()">
                      <span id="nameerr" class="text-danger"></span>
                       <label for="">Offer</label>
                       <input type="text" name="Offer" class="form-control" id="Offer" >
                    
                    {{!-- <label for="">Image</label>
                    <img src="/productimages/{{product.images.[0]}} " alt="" style="width:100px ;height:auto" id="imgView"
                        class="pt-3">
                    <input type="file" name="Image" class="form-control" required onchange="viewImage(event)"> --}}

                    <button type="submit" class="btn btn-success mt-4" onclick="return validation()">submit
                      
                    </button>


                </form>
            </div>

        </div>
    </div>
</section>
<script>
    function viewImage(event) {
        document.getElementById('imgView').src = URL.createObjectURL(event.target.files[0])
    }
</script>
<script>
    var errorName = document.getElementById('nameerr')
    var errorcategory = document.getElementById('categoryy')
    var errorprice = document.getElementById('priceee')
    var errorDiscription=document.getElementById('discription')
    function validateName() {
        const name = document.getElementById('Name').value;
        if (name == "") {
            errorName.innerHTML = 'Enter your Name'
            return false
        }
        if (!name.match(/^[a-zA-Z]*$/)) {
            errorName.innerHTML = 'Alphanets are only allowed'
            return false
        }
        errorName.innerHTML = null
        return true

    }
    function validCategory() {
        const name = document.getElementById('category').value;
        if (name == "") {
            errorcategory.innerHTML = 'Enter your category'
            return false
        }
        if (!name.match(/^[a-zA-Z]*$/)) {
            errorcategory.innerHTML = 'Alphanets are only allowed'
            return false
        }
        errorName.innerHTML = null
        return true

    }
function validDiscription() {
        const name = document.getElementById('Description').value;
        if (name == "") {
            errorName.innerHTML = 'Enter your Discription'
            return false
        }
        if (!name.match(/^[a-zA-Z]*$/)) {
            errorName.innerHTML = 'Alphanets are only allowed'
            return false
        }
        errorName.innerHTML = null
        return true

    }
     function validPrice() {
        const number = document.getElementById('price').value
        if (number == "") {
            errorprice.innerHTML = "enter your price"
            return false
        }
        if (!number.match(/^\d{10}$/)) {
            errorprice.innerHTML = 'Number should be valid'
            return false
        }
        errorNumber.innerHTML = null
        return true
    }


    function validation() {
        if (!validateName() || !validateCategory() || !validateDiscription() || !validPrice()) {
            return false
        }
        return true
    }
</script>